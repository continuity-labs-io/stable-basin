# The Measurement Problem (The Observer Model)

**Status:** Completed

## 1. The Conceptual Shift

The 129-D Quintet Tensor is the theoretical manifold. Practically, measuring
Transcriptomics ($\Psi$) and Epigenetics ($\Gamma$) requires lysing (killing)
the cell.

## 2. The Narrative

The Invariant Core continuously ingests the Non-Destructive Trio (Voltage,
Morphology, Fluid Exhaust). It uses Mask-Aware Subspace Routing to
mathematically infer the hidden, destructive states (RNA, Epigenetics) from the
continuous exhaust, allowing the cell to live.

## 3. The Repo Fix (Implementation Plan)

This is the ultimate validation of the current focus on `mask_aware_ssm.py`. We
will explicitly rename its role in the documentation to the Observer Model.

### Action Items

- [x] Explicitly rename the role of `mask_aware_ssm.py` in the documentation to
      the "Observer Model".

### Targeted Files

- `mask_aware_ssm.py`
