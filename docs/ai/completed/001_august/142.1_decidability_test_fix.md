ROLE: You are an elite Scientific Machine Learning Engineer.

TASK: Fix the critical physics sign error and the "Open-Loop Overshoot" bug in the Decidability Diagnostic. 

Your implementation report was a fantastic confession. You realized the physics engine was exploding, so you cleverly inverted the energy landscape (`E(x) = -0.5 * ||x||^2`) to make it a hill, which stabilized the math. Furthermore, the V-shape in the plot is caused by broadcasting a constant restorative force (`q_pulse`) across 200 timesteps, blasting the patient through the origin and into deep negative space.

We must fix the core physics and upgrade the Bio-Blade to a true Closed-Loop Proportional Controller that ONLY acts on the Markov Blanket.

This requires updates to 4 files:

### PART 1: The Physics Sign Error (`src/echo/physics/thermostat.py`)
- The current equation `drift = -(Q - Gamma) @ grad_E` turns the positive-definite `Gamma` into an accelerator (pushing UP the energy gradient). 
- Change the drift equation to: `drift = -(Q + Gamma) @ grad_E` (so `Gamma` acts as a true brake).

### PART 2: Closed-Loop Control (`src/echo/primitives/thermalizer.py`)
- Modify `ForcedTorxThermalizer.__call__`. Replace the `q_seq` argument with `q_gain: float = 0.0` and `q_mask: jax.Array | None = None`.
- Inside `step_fn`, compute the dynamic closed-loop force:
  ```python
  current_q_mask = q_mask if q_mask is not None else jnp.ones_like(state)
  q_ext = -q_gain * state * current_q_mask

```

* Pass this `q_ext` into the `inputs` dict for the factor instead of reading from a sequence.

### PART 3: Hierarchical Propagations (`src/echo/architecture/observer.py` & `hierarchy.py`)

* Update the `forced_unroll` signatures in both `MarkovBlanketObserver` and `PredictiveCodingGraph` to accept `q_gain: float = 0.0` and `q_mask: jax.Array | None = None` instead of `q_seq`.
* Pass these down to the `forced_thermalizer`.

### PART 4: The Benchmark Fix (`src/echo/benchmarks/bioblade_diagnostic.py`)

1. **Fix the EBM**: Change `QuadraticEBM` to return a true basin: `return 0.5 * jnp.sum(x**2), jnp.eye(self.d_state)`.
2. **Implement Blanket-Only Control**: The Bio-Blade is an external device; it cannot reach into the cell. It can ONLY stimulate the Markov Blanket (Sensory + Active states).
* Create a `q_mask` of shape `(d_state,)`. Set it to `1.0` for the `sensory` and `active` indices, and `0.0` for `internal` and `external`.


3. **Execute Phase 2**:
* Remove the `q_seq_a` and `q_seq_b` logic.
* Run Phase 2 using the closed-loop controller: `q_gain=10.0` and `q_mask=q_mask`.


4. **Thresholds**: Leave the distance evaluation as the L2 norm of the full state (`final_a` and `final_b`). Set `rescue_threshold = 1.0`.

TESTING:
Ensure all tests in `tests/echo/` are updated to match the new `q_gain`/`q_mask` signatures and pass.


### Expected Output
When this runs, the V-shape of death will vanish entirely. 

The plot for **Patient A** will hit the red dashed line (the intervention) and swoop down in a beautiful, asymptotic exponential decay curve, flatlining near zero. The Bio-Blade controls the blanket, and Patient A's healthy internal physics drag the core to safety.

The plot for **Patient B** will hit the red dashed line, dip slightly as the Bio-Blade fights the sensors, and then peel back up into the stratosphere as the internal cells physically detach from the network and die.
