Load src/demo/00_trivial_toy_overfit.py.

Import SpectralMetrics from src.metrics.spectral.

In the evaluation section, extract the predicted macro (slow, Channel 0) and micro (fast, Channel 1) channels.

Instantiate SpectralMetrics and calculate the Phase-Amplitude Coupling (PAC) using calculate_cfc_pac. 

To visualize when the biological structure decouples, calculate the PAC over a rolling window and plot it. Add a 3rd subplot panel showing this rolling window of the PAC score over time, demonstrating the exact moment of decoupling at the simulated crash (T=70).

Print the Phase-Amplitude Coupling (MVL) during prediction to the console.
