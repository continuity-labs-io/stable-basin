**Context & Goal:**
We have successfully run the Dose-Response Lambda Sweep (`09_worm_gait_lambda_sweep.py`). Now, we are executing Part 2 of our paper finalization strategy: "Translating $\lambda$ for the Clinic."

We need to translate our abstract precision-gain parameter ($\lambda$) into standard pharmacological metrics so clinicians can compare our "digital intervention" to real-world small molecules. To do this, we need to extract the $EC_{50}$ from our dose-response curve and calculate the equivalent Gibbs Free Energy ($\Delta G$) shift required to rescue the Waddington basin.

**Your Task:**
Create a new benchmark script: `src/benchmarks/10_pharmacological_translation.py`

**Requirements:**
1. **Sigmoid Fitting ($EC_{50}$):** 
   - Load the JSON data generated by the previous step from `output/echo/benchmarks/09_lambda_sweep_metrics.json`.
   - Use `scipy.optimize.curve_fit` to fit a standard 4-parameter logistic (4PL) Hill equation (a sigmoidal dose-response curve) to the $\lambda$ (dose) vs. Hessian Trace (response) data.
   - Extract the $EC_{50}$ value (the $\lambda$ at which 50% of the maximum basin steepening is achieved).
2. **The Physics Engine:** 
   - Reuse the SDE integration logic from `07_worm_gait_intervention.py`. Load the trained `06_worm_gait_decline_trained_engine.eqx` weights and the Old worm dataset (`EigenWorms_TEST.ts`, `is_aged=True`).
3. **The Trajectories:** 
   - Run the SDE integration for two conditions:
     * **Pathological Baseline:** $\lambda_{base} = 0.2$
     * **Therapeutic Rescue:** $\lambda_{rescue} = EC_{50}$ (using the float value calculated in Step 1).
4. **Thermodynamic Translation ($\Delta G$):**
   - For both resulting trajectory batches, evaluate the scalar Energy of every state in the trajectory. You can use the `joint_energy_fn(x_micro, x_macro)` from the `HierarchicalThermoFlowFactor`.
   - Calculate the thermodynamic work (Activation Energy) provided by the intervention: 
     * $G_{baseline} = \lambda_{base} \times \text{mean}(E_{joint}(x_{baseline}))$
     * $G_{rescue} = \lambda_{rescue} \times \text{mean}(E_{joint}(x_{rescue}))$
     * $\Delta G = \vert{} G_{rescue} - G_{baseline} \vert{}$
5. **Serialization:** Save these metrics to `output/echo/benchmarks/10_clinical_translation_metrics.json` containing the calculated $EC_{50}$, the Hill slope, the maximum trace asymptote, and the $\Delta G$ energy shift.
6. **Visualization:** Generate `output/echo/benchmarks/10_pharmacological_curve.png`.
   - Plot the raw $\lambda$ vs Trace points as a scatter plot (use a logarithmic X-axis for the doses).
   - Overlay the smooth, fitted Sigmoid curve.
   - Add a vertical dashed line and a text box annotation explicitly marking the $EC_{50}$ threshold and printing the calculated $\Delta G$ energy shift.
7. **Pipeline Update:** Add a `.PHONY: worm-gait-pharmacology` target to the `Makefile` executing this script, and append it to the `worm-gait-experiments` chain.

**Constraints:**
- No new neural network training is required here. This is pure scipy curve fitting and evaluating the existing energy functions via SDE unrolling.
- Ensure the Hill equation handles the bounds correctly so `curve_fit` doesn't throw a `RuntimeError`.

Please execute this and report the final $EC_{50}$ and $\Delta G$ values!
