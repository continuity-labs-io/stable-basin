ROLE: You are an elite Scientific Machine Learning Engineer.

TASK: Enhance the `HessianCurvatureTracker` to explicitly calculate the "Nullity" and "Rank" of the biological attractor basin, and update the Contention Benchmark to visualize it.

### PART 1: The Math (src/echo/metrics/thermal_interpretability.py)
Update the `HessianCurvatureTracker.calculate_curvature(self, x)` method to return two new metrics:
1. `hessian_rank`: The number of mathematically significant restoring forces (eigenvalues > 1e-4). This quantifies Route Diversity (how many distinct ways the system can repair itself).
2. `hessian_nullity`: The number of "flat" or dead directions (eigenvalues <= 1e-4). This quantifies the number of exhausted/degenerate pathways.

Add these computations right after calculating the `eigenvalues`:
```python
threshold = 1e-4
# eigenvalues is already computed via jnp.linalg.eigvalsh
hessian_rank = jnp.sum(jnp.abs(eigenvalues) > threshold)
hessian_nullity = eigenvalues.shape[0] - hessian_rank
```

Ensure these are returned in the dictionary as integer arrays (or floats if required by JAX VMAP).

### PART 2: The Benchmark (src/echo/benchmarks/concurrent_contention_benchmark.py)

Update the benchmark script to utilize this new metric to prove the theory of Contention.

* When you run `HessianCurvatureTracker.batch_calculate_curvature(traj)` for Phase A, Phase B, and Phase C, extract the `hessian_nullity` array for each.
* In the Matplotlib output (`output/echo/contention_benchmark.png`), change the layout to 2 subplots (stacked vertically):

- * **Top Plot:** The Physical State Divergence (L2 Norm) over time for Phase A, B, and C.
- * **Bottom Plot:** The "Route Diversity Collapse" (Hessian Nullity) over time for Phase A, B, and C.


* **The Visual Proof:**
- * In Phase A and B, the Nullity should remain relatively stable or shift smoothly as the single stressor engages the pathways.
- * In Phase C (Contention), when both stressors hit simultaneously, the Nullity should spike or behave erratically. The shared restorative pathways are overwhelmed, the network's local Jacobian loses full rank, and the organism loses its dimensions of freedom.


* Update the terminal printout to explicitly report the Final Hessian Rank and Nullity for A, B, and C.

TESTING:
Ensure all existing tests in `test_thermal_interpretability.py` are updated to expect/handle the new `hessian_rank` and `hessian_nullity` keys in the returned metric dictionary. Add a test passing a dummy EBM with a perfectly flat energy function (e.g., `E(x) = 0.0`) to prove the nullity evaluates to exactly `d_state`.


### Why this is scientifically profound:
If the benchmark produces the plot we expect, you will have computationally proven **Pleiotropy and Contention**. 

In biology, genes and pathways are pleiotropic (they do multiple things). When a cell is young and unstressed, it has 32 dimensions of freedom (Rank = 32, Nullity = 0). It can handle a heat shock (Stress A) by using pathways 1-10. It can handle a poison (Stress B) using pathways 5-15. 

Because pathways 5-10 are shared, if you hit the cell with heat AND poison at the same time, the math of the neural network will max out those specific hidden neurons. The Jacobian flattens out in those directions. The Hessian Rank collapses, and the Nullity spikes. 

